<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>DOM on 文翼的博客</title>
    <link>http://blog.wenzhixin.net.cn/tags/dom/</link>
    <description>Recent content in DOM on 文翼的博客</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <copyright>wenzhixin.net.cn 粤ICP备15117953号</copyright>
    <atom:link href="http://blog.wenzhixin.net.cn/tags/dom/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>《锋利的jQuery》学习笔记——jQuery中的DOM操作</title>
      <link>http://blog.wenzhixin.net.cn/1/01/01/sharp_jquery_study_notes_dom_operation_in_jquery</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>http://blog.wenzhixin.net.cn/1/01/01/sharp_jquery_study_notes_dom_operation_in_jquery</guid>
      <description>&lt;p&gt;​&lt;strong&gt;1. 什么是DOM？&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt; • DOM 是 Document Object Model 的缩写，是指文档对象模型&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;​2. DOM操作的分类&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)DOM Core-可用来处理任何一种使用标记语言的文档&lt;/p&gt;

&lt;p&gt; • getElementById()&lt;/p&gt;

&lt;p&gt; • getElementByTagName()&lt;/p&gt;

&lt;p&gt; • getAttribute()&lt;/p&gt;

&lt;p&gt; • setAttribute()&lt;/p&gt;

&lt;p&gt;(2)HTML_DOM-提供一些简明的记号来描述各种 HTML 元素的属性&lt;/p&gt;

&lt;p&gt;(3)CSS_DOM-针对 CSS 的操作，主要是获取和设置 style 对象的各种属性&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;​3. jQuery中的DOM操作&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)查找节点-通过 jQuery 选择器来进行查找&lt;/p&gt;

&lt;p&gt;(2)创建节点-使用 jQuery 的工厂函数$()来创建&lt;/p&gt;

&lt;p&gt; • $(html)&lt;/p&gt;

&lt;p&gt;(3)插入节点-不仅能创建新的 DOM 元素，也能对原有的 DOM 元素进行移动&lt;/p&gt;

&lt;p&gt; • append()-向每个匹配的元素内部追加内容&lt;/p&gt;

&lt;p&gt; • appendTo()-将所有匹配的元素追加到指定的元素中，与 append 函数相反&lt;/p&gt;

&lt;p&gt; • prepend()-向每个匹配的元素内部前置内容&lt;/p&gt;

&lt;p&gt; • prependTo()-将所有匹配的元素前置到指定的元素中，与 prepend 函数相反&lt;/p&gt;

&lt;p&gt; • after()-在每个匹配的元素之后插入内容&lt;/p&gt;

&lt;p&gt; • insertAfter()-将所有匹配的元素插入到指定元素的后面，与 after 函数相反&lt;/p&gt;

&lt;p&gt; • before()-在每个匹配的元素之前插入内容&lt;/p&gt;

&lt;p&gt; • insertBefore()-将所有匹配的元素插入到指定的元素的前面，与 before
函数相反&lt;/p&gt;

&lt;p&gt;(4)删除和清空节点&lt;/p&gt;

&lt;p&gt; • remove()-删除所匹配的元素&lt;/p&gt;

&lt;p&gt; • empty()-清空所匹配元素的所有后代节点&lt;/p&gt;

&lt;p&gt;(5)复制节点&lt;/p&gt;

&lt;p&gt; • clone(flag)-flag 表示复制元素的同时是否复制元素中所绑定的事件&lt;/p&gt;

&lt;p&gt;(6)替换节点&lt;/p&gt;

&lt;p&gt; • replaceWith()-将所有匹配的元素都替换成指定的 HTML 或者 DOM 元素&lt;/p&gt;

&lt;p&gt; • replaceAll()-与 replaceWith 函数相反&lt;/p&gt;

&lt;p&gt;(7)包裹节点&lt;/p&gt;

&lt;p&gt; • wrapAll()-将所有匹配的元素用一个元素包裹起来&lt;/p&gt;

&lt;p&gt; • wrap()-将所有的匹配的元素进行单独的包裹&lt;/p&gt;

&lt;p&gt; • wrapInner()-将每一个匹配的元素的子内容用其他结构化的标记包裹起来&lt;/p&gt;

&lt;p&gt;​&lt;strong&gt;4. 属性操作&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)获取和设置属性&lt;/p&gt;

&lt;p&gt; • attr(name)-获取属性 name 的值&lt;/p&gt;

&lt;p&gt; • attr(name, value)-设置属性 name 的值为 value&lt;/p&gt;

&lt;p&gt;(2)删除属性&lt;/p&gt;

&lt;p&gt; • removeAttr(name)-删除属性 name&lt;/p&gt;

&lt;p&gt;​&lt;strong&gt;5. 样式操作&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)获取和设置样式&lt;/p&gt;

&lt;p&gt; • css(name)-获取 css 属性 name 的值&lt;/p&gt;

&lt;p&gt; • css(name, value)-设置 css 属性 name 的值为 value&lt;/p&gt;

&lt;p&gt;(2)追加和移除样式&lt;/p&gt;

&lt;p&gt; • addClass(className)-追加 className 样式到指定元素&lt;/p&gt;

&lt;p&gt; • removeClass(className)-从指定的元素中移除 className 样式&lt;/p&gt;

&lt;p&gt;(3)切换样式&lt;/p&gt;

&lt;p&gt; • toggle(className)-交互切换 clssName 样式&lt;/p&gt;

&lt;p&gt;(4)判断样式&lt;/p&gt;

&lt;p&gt; • hasClass(className)-判断是否含有 className 样式&lt;/p&gt;

&lt;p&gt;​&lt;strong&gt;6. 设置和获取 HTML、文本和表单的值&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)HTML&lt;/p&gt;

&lt;p&gt; • html()-获取指定元素的 html&lt;/p&gt;

&lt;p&gt; • html(html)-设置指定元素的 html&lt;/p&gt;

&lt;p&gt;(2)文本&lt;/p&gt;

&lt;p&gt; • text()-获取指定元素的文本内容&lt;/p&gt;

&lt;p&gt; • text(text)-设置指定元素的文本内容&lt;/p&gt;

&lt;p&gt;(3)表单&lt;/p&gt;

&lt;p&gt; • val()-获取表单的值&lt;/p&gt;

&lt;p&gt; • val(value)-设置表单的值为 value&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;7.遍历节点&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt; • children()-用于取得匹配元素的子元素集合&lt;/p&gt;

&lt;p&gt; • next()-用于取得匹配元素后面紧邻的同辈元素&lt;/p&gt;

&lt;p&gt; • prev()-用于取得匹配元素前面紧邻的同辈元素&lt;/p&gt;

&lt;p&gt; • siblings()-用于取得匹配元素前后所有的同辈元素&lt;/p&gt;

&lt;p&gt; • closest()-取得最近的匹配元素&lt;/p&gt;

&lt;p&gt; • find()、filter()、nextAll()、prevAll()、parent()、parents()&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;8.CSS_DOM 中经常使用的方法&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt; • width()-获取元素的宽度&lt;/p&gt;

&lt;p&gt; • height()-获取元素的高度&lt;/p&gt;

&lt;p&gt; • offset()-获取元素在当前视窗的相对偏移&lt;/p&gt;

&lt;p&gt; • position()-获取元素相对于最近的一个 position 样式属性设置的 relative
或者 absolute 的祖父节点的相对偏移&lt;/p&gt;

&lt;p&gt; • scrollTop()-获取元素的滚动条距离顶端的距离&lt;/p&gt;

&lt;p&gt; • scrollLeft-获取元素的滚动条距离左侧的距离&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>《锋利的jQuery》学习笔记——jQuery中的DOM操作</title>
      <link>http://blog.wenzhixin.net.cn/1/01/01/sharp_jquery_study_notes_dom_operation_in_jquery</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      
      <guid>http://blog.wenzhixin.net.cn/1/01/01/sharp_jquery_study_notes_dom_operation_in_jquery</guid>
      <description>&lt;p&gt;​&lt;strong&gt;1. 什么是DOM？&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt; • DOM 是 Document Object Model 的缩写，是指文档对象模型&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;​2. DOM操作的分类&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)DOM Core-可用来处理任何一种使用标记语言的文档&lt;/p&gt;

&lt;p&gt; • getElementById()&lt;/p&gt;

&lt;p&gt; • getElementByTagName()&lt;/p&gt;

&lt;p&gt; • getAttribute()&lt;/p&gt;

&lt;p&gt; • setAttribute()&lt;/p&gt;

&lt;p&gt;(2)HTML_DOM-提供一些简明的记号来描述各种 HTML 元素的属性&lt;/p&gt;

&lt;p&gt;(3)CSS_DOM-针对 CSS 的操作，主要是获取和设置 style 对象的各种属性&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;​3. jQuery中的DOM操作&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)查找节点-通过 jQuery 选择器来进行查找&lt;/p&gt;

&lt;p&gt;(2)创建节点-使用 jQuery 的工厂函数$()来创建&lt;/p&gt;

&lt;p&gt; • $(html)&lt;/p&gt;

&lt;p&gt;(3)插入节点-不仅能创建新的 DOM 元素，也能对原有的 DOM 元素进行移动&lt;/p&gt;

&lt;p&gt; • append()-向每个匹配的元素内部追加内容&lt;/p&gt;

&lt;p&gt; • appendTo()-将所有匹配的元素追加到指定的元素中，与 append 函数相反&lt;/p&gt;

&lt;p&gt; • prepend()-向每个匹配的元素内部前置内容&lt;/p&gt;

&lt;p&gt; • prependTo()-将所有匹配的元素前置到指定的元素中，与 prepend 函数相反&lt;/p&gt;

&lt;p&gt; • after()-在每个匹配的元素之后插入内容&lt;/p&gt;

&lt;p&gt; • insertAfter()-将所有匹配的元素插入到指定元素的后面，与 after 函数相反&lt;/p&gt;

&lt;p&gt; • before()-在每个匹配的元素之前插入内容&lt;/p&gt;

&lt;p&gt; • insertBefore()-将所有匹配的元素插入到指定的元素的前面，与 before
函数相反&lt;/p&gt;

&lt;p&gt;(4)删除和清空节点&lt;/p&gt;

&lt;p&gt; • remove()-删除所匹配的元素&lt;/p&gt;

&lt;p&gt; • empty()-清空所匹配元素的所有后代节点&lt;/p&gt;

&lt;p&gt;(5)复制节点&lt;/p&gt;

&lt;p&gt; • clone(flag)-flag 表示复制元素的同时是否复制元素中所绑定的事件&lt;/p&gt;

&lt;p&gt;(6)替换节点&lt;/p&gt;

&lt;p&gt; • replaceWith()-将所有匹配的元素都替换成指定的 HTML 或者 DOM 元素&lt;/p&gt;

&lt;p&gt; • replaceAll()-与 replaceWith 函数相反&lt;/p&gt;

&lt;p&gt;(7)包裹节点&lt;/p&gt;

&lt;p&gt; • wrapAll()-将所有匹配的元素用一个元素包裹起来&lt;/p&gt;

&lt;p&gt; • wrap()-将所有的匹配的元素进行单独的包裹&lt;/p&gt;

&lt;p&gt; • wrapInner()-将每一个匹配的元素的子内容用其他结构化的标记包裹起来&lt;/p&gt;

&lt;p&gt;​&lt;strong&gt;4. 属性操作&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)获取和设置属性&lt;/p&gt;

&lt;p&gt; • attr(name)-获取属性 name 的值&lt;/p&gt;

&lt;p&gt; • attr(name, value)-设置属性 name 的值为 value&lt;/p&gt;

&lt;p&gt;(2)删除属性&lt;/p&gt;

&lt;p&gt; • removeAttr(name)-删除属性 name&lt;/p&gt;

&lt;p&gt;​&lt;strong&gt;5. 样式操作&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)获取和设置样式&lt;/p&gt;

&lt;p&gt; • css(name)-获取 css 属性 name 的值&lt;/p&gt;

&lt;p&gt; • css(name, value)-设置 css 属性 name 的值为 value&lt;/p&gt;

&lt;p&gt;(2)追加和移除样式&lt;/p&gt;

&lt;p&gt; • addClass(className)-追加 className 样式到指定元素&lt;/p&gt;

&lt;p&gt; • removeClass(className)-从指定的元素中移除 className 样式&lt;/p&gt;

&lt;p&gt;(3)切换样式&lt;/p&gt;

&lt;p&gt; • toggle(className)-交互切换 clssName 样式&lt;/p&gt;

&lt;p&gt;(4)判断样式&lt;/p&gt;

&lt;p&gt; • hasClass(className)-判断是否含有 className 样式&lt;/p&gt;

&lt;p&gt;​&lt;strong&gt;6. 设置和获取 HTML、文本和表单的值&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;(1)HTML&lt;/p&gt;

&lt;p&gt; • html()-获取指定元素的 html&lt;/p&gt;

&lt;p&gt; • html(html)-设置指定元素的 html&lt;/p&gt;

&lt;p&gt;(2)文本&lt;/p&gt;

&lt;p&gt; • text()-获取指定元素的文本内容&lt;/p&gt;

&lt;p&gt; • text(text)-设置指定元素的文本内容&lt;/p&gt;

&lt;p&gt;(3)表单&lt;/p&gt;

&lt;p&gt; • val()-获取表单的值&lt;/p&gt;

&lt;p&gt; • val(value)-设置表单的值为 value&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;7.遍历节点&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt; • children()-用于取得匹配元素的子元素集合&lt;/p&gt;

&lt;p&gt; • next()-用于取得匹配元素后面紧邻的同辈元素&lt;/p&gt;

&lt;p&gt; • prev()-用于取得匹配元素前面紧邻的同辈元素&lt;/p&gt;

&lt;p&gt; • siblings()-用于取得匹配元素前后所有的同辈元素&lt;/p&gt;

&lt;p&gt; • closest()-取得最近的匹配元素&lt;/p&gt;

&lt;p&gt; • find()、filter()、nextAll()、prevAll()、parent()、parents()&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;8.CSS_DOM 中经常使用的方法&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt; • width()-获取元素的宽度&lt;/p&gt;

&lt;p&gt; • height()-获取元素的高度&lt;/p&gt;

&lt;p&gt; • offset()-获取元素在当前视窗的相对偏移&lt;/p&gt;

&lt;p&gt; • position()-获取元素相对于最近的一个 position 样式属性设置的 relative
或者 absolute 的祖父节点的相对偏移&lt;/p&gt;

&lt;p&gt; • scrollTop()-获取元素的滚动条距离顶端的距离&lt;/p&gt;

&lt;p&gt; • scrollLeft-获取元素的滚动条距离左侧的距离&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>